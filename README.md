# pibit-prompt-optimizer
Production-grade multi-agent framework implementing an automated Critic-Mutator loop for optimizing structured JSON extraction prompts from ExtractBench PDFs. Features deterministic data splitting, deterministic scoring configs, persistent SQLite state tracking, automated markdown diffing, and zero-cost local/cloud LLM orchestration.
