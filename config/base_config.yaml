dataset:
  name: "hiring/resume"
  base_path: "./data/extract-bench"
  split_seed: 42
  train_ratio: 0.6
  val_ratio: 0.2

budget:
  max_iterations: 20  # Keep your iteration limit
  max_cost_dollars: 100.0  # Set high so the 0.0 >= 0.0 condition doesn't trigger

models:
  extractor: "gemini-2.5-flash"
  critic: "gemini-2.5-flash"
  mutator: "gemini-2.5-pro"
  
seed_prompt: |
  Extract the structured information from the provided document.
  Return ONLY valid JSON matching the exact schema. Do not include markdown formatting like ```json.