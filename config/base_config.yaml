dataset:
  name: "hiring/resume"
  base_path: "./data/extract-bench"
  split_seed: 42
  train_ratio: 0.6
  val_ratio: 0.2

budget:
  max_iterations: 20
  max_cost_dollars: 0.0

models:
  extractor: "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free"
  critic: "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free"
  mutator: "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free"

seed_prompt: |
  Extract the structured information from the provided document.
  Return ONLY valid JSON matching the exact schema. Do not include markdown formatting like ```json.